<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Health Chats</title>
    <link rel="stylesheet" href="style.css"/>
</head>
<body>
    <h1>Health Tracking App</h1>
    <h3>DASHBOARD</h3>



    
    <form method="GET" action="/chats/new">
        <button>Create New Record</button>
    </form>
    <br>
    <br>

    <!-- Table structure for displaying health records -->
    <table border="1" id="ttt">
        <thead>
            <tr>
                <th>Date</th>
                <th>Temperature (°C)</th>
                <th>Blood Pressure</th>
                <th>Heart Rate (BPM)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% for (let chat of chats) { %>
                <tr>
                    <td><%= new Date(chat.Date).toLocaleDateString() %></td>
                    <td><%= chat.temperature %></td>
                    <td><%= chat.bp %></td>
                    <td><%= chat.bpm %></td>
                    <td>
                        <form method="GET" action="/chats/<%= chat._id %>/edit" style="display:inline;">
                            <button>Edit</button>
                        </form>
                        <form method="POST" action="/chats/<%= chat._id %>?_method=DELETE" style="display:inline;">
                            <button>Delete</button>
                        </form>
                    </td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>
